<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/presetRowName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
            android:textColor="?attr/colorOnSurface"
            android:singleLine="true"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/presetRowPreamp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAppearance="@style/TextAppearance.Material3.BodySmall"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:textSize="11sp"
            android:singleLine="true"
            android:visibility="gone" />
    </LinearLayout>

    <com.maxxcodebug.maxxequalizer.ui.PresetCurveView
        android:id="@+id/presetRowCurve"
        android:layout_width="64dp"
        android:layout_height="28dp"
        android:layout_marginStart="12dp" />

    <!-- Shown in place of the curve for the "Disable EQ" entry (no
         curve to draw). Toggled with presetRowCurve in the adapter. -->
    <TextView
        android:id="@+id/presetRowNA"
        android:layout_width="64dp"
        android:layout_height="28dp"
        android:layout_marginStart="12dp"
        android:gravity="center"
        android:text="N/A"
        android:textColor="#FFEF9A9A"
        android:textSize="12sp"
        android:visibility="gone" />
</LinearLayout>
