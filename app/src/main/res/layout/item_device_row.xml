<?xml version="1.0" encoding="utf-8"?>
<!-- Device row. Single outlined card wraps the drag handle (left) and
     the device label + preset dropdown (right). Handle lives entirely
     inside the outline. Right padding equals the left effective inset
     (handle + spacing) so the device-name text and preset dropdown
     sit at the same distance from the card border on both sides. -->
<com.maxxcodebug.maxxequalizer.ui.NotchedDeviceCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/deviceRowCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardBackgroundColor="@android:color/transparent"
    app:cardCornerRadius="16dp"
    app:cardElevation="0dp"
    app:strokeColor="?attr/colorOutline"
    app:strokeWidth="1dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp">

        <ImageView
            android:id="@+id/deviceRowDragHandle"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:padding="4dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_menu_handle"
            android:background="@drawable/handle_ripple"
            android:clickable="true"
            android:focusable="true"
            android:contentDescription="Drag handle" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/deviceRowName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:singleLine="true"
                android:ellipsize="end"
                android:textAppearance="@style/TextAppearance.Material3.TitleSmall"
                android:textColor="?attr/colorOnSurface" />

            <TextView
                android:id="@+id/deviceRowKey"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:singleLine="true"
                android:ellipsize="end"
                android:textAppearance="@style/TextAppearance.Material3.BodySmall"
                android:textColor="?attr/colorOnSurfaceVariant" />

            <!-- Preset dropdown — SAME style + widget hierarchy as the
                 Current Device card so the two look identical. -->
            <com.google.android.material.textfield.TextInputLayout
                android:id="@+id/deviceRowPresetLayout"
                style="@style/Widget.Material3.TextInputLayout.OutlinedBox.ExposedDropdownMenu"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:hint="Preset"
                android:clickable="true"
                android:focusable="true"
                android:textColorHint="?attr/colorOnSurfaceVariant"
                app:boxStrokeColor="@color/dropdown_box_stroke"
                app:boxStrokeWidth="1dp"
                app:boxStrokeWidthFocused="1dp"
                app:endIconTint="@color/dropdown_end_icon_tint"
                app:hintTextColor="?attr/colorOnSurfaceVariant">

                <com.maxxcodebug.maxxequalizer.ui.DropdownAutoCompleteTextView
                    android:id="@+id/deviceRowPresetDropdown"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:clickable="false"
                    android:cursorVisible="false"
                    android:focusable="false"
                    android:inputType="none"
                    android:longClickable="false"
                    android:textIsSelectable="false" />
            </com.google.android.material.textfield.TextInputLayout>
        </LinearLayout>

        <!-- Right-side action buttons. No-op for now (UI placement).
             Both use Outlined style so the "inside" of each is the
             same (transparent over the card surface); the X is
             distinguished by a red icon tint. 32dp × 32dp squares to
             leave the dropdown the same width on both sides. -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginStart="8dp"
            android:gravity="center">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/deviceRowRemoveButton"
                style="@style/Widget.Material3.Button.IconButton.Outlined"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:minWidth="0dp"
                android:minHeight="0dp"
                android:padding="0dp"
                android:insetLeft="0dp"
                android:insetTop="0dp"
                android:insetRight="0dp"
                android:insetBottom="0dp"
                app:cornerRadius="10dp"
                app:icon="@drawable/ic_close"
                app:iconSize="14dp"
                app:iconTint="#FFEF9A9A"
                app:iconPadding="0dp"
                app:iconGravity="textStart"
                android:contentDescription="Remove device" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/deviceRowHideButton"
                style="@style/Widget.Material3.Button.IconButton.Outlined"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_marginTop="6dp"
                android:minWidth="0dp"
                android:minHeight="0dp"
                android:padding="0dp"
                android:insetLeft="0dp"
                android:insetTop="0dp"
                android:insetRight="0dp"
                android:insetBottom="0dp"
                app:cornerRadius="10dp"
                app:icon="@drawable/ic_visibility"
                app:iconSize="14dp"
                app:iconPadding="0dp"
                app:iconGravity="textStart"
                android:contentDescription="Toggle visibility" />
        </LinearLayout>
    </LinearLayout>
</com.maxxcodebug.maxxequalizer.ui.NotchedDeviceCardView>
